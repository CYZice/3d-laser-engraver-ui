##  三维激光内雕 前端设计

<progress value="10" max="100" style="width: 100%;"></progress>
# 第一部分：产品需求文档 (PRD)

## 1. 项目概述
构建一个基于Web的图像处理工具，用户上传位图（JPG/PNG），在前端进行基础编辑（裁切/旋转）后，提交给后端转换为矢量点云（DXF）。

## 2. 详细功能流程

该系统是一个典型的**线性流程（Linear Workflow）**，分为四个阶段：

### 阶段一：待机与上传 (Idle & Upload)
*   **交互：** 页面中心显示一个大的虚线框（Dropzone）。
*   **功能：**
    *   支持点击触发文件选择器。
    *   支持将文件直接拖拽入框内。
    *   **限制：** 仅限 `.jpg`, `.png`, `.jpeg` 格式；文件大小限制在 20MB 以内（避免浏览器卡顿）。
    *   **校验：** 如果格式或大小不符，弹出红色错误提示。

### 阶段二：预处理/编辑 (Preprocessing)
*   **交互：** 上传成功后，界面切换为编辑器模式。
*   **功能：**
    *   **画布：** 显示上传的图片。
    *   **裁剪：** 提供一个矩形裁剪框，用户可拖动四个角调整范围。
    *   **旋转：** 提供“向左旋转”和“向右旋转”按钮（每次90度）。
    *   **操作栏：** “重置”按钮（恢复原图）、“确认转换”按钮。

### 阶段三：处理与反馈 (Processing)
*   **交互：** 点击“确认转换”后，界面遮罩或进入Loading状态，不可进行其他操作。
*   **功能：**
    *   **进度条：** 实时显示 0% -> 100% 的进度。
    *   **状态文案：** 显示当前后端正在进行的步骤（如：正在上传 -> 正在提取轮廓 -> 正在生成DXF）。
    *   **异常处理：** 如果后端报错，显示错误信息并提供“重试”按钮。

### 阶段四：结果预览与交付 (Result)
*   **交互：** 处理完成后，显示结果面板。
*   **功能：**
    *   **预览：** 可视化展示生成的 DXF 内容（详见实现方案）。
    *   **对比：** 提供开关，允许用户叠加查看 原图 vs DXF线条。
    *   **下载：** 醒目的“下载 .dxf 文件”按钮。
    *   **重来：** “处理下一张”按钮，点击后回到阶段一。

---

# 第二部分：技术选型

为了保证开发效率、运行稳定性和后续扩展性，推荐使用以下成熟的现代前端技术栈。

| 模块 | 推荐技术 | 选择理由 |
| :--- | :--- | :--- |
| **语言** | **TypeScript** | 强类型，避免图像处理参数传递时的低级错误。 |
| **框架** | **React 18+** | 组件化强，生态最丰富，适合工具类应用。 |
| **构建工具** | **Vite** | 开发启动秒开，打包体积小。 |
| **UI 组件库** | **Ant Design (v5)** | 自带高质量的 `Upload` (拖拽上传)、`Progress` 和 `Steps` 组件，风格专业。 |
| **图片编辑库** | **react-cropper** | 基于 Cropper.js 的最强封装，支持高性能的 Canvas 裁剪和旋转。 |
| **HTTP 客户端** | **Axios** | 处理上传进度监控和响应拦截非常方便。 |
| **状态管理** | **Zustand** | 比 Redux 轻量得多，非常适合这种中小型应用的状态流转。 |
| **DXF 渲染(扩展)**| **three-dxf** / **three.js** | 浏览器端 3D/CAD 渲染的标准方案。 |

---

# 第三部分：代码架构设计

## 1. 目录结构
```text
src/
├── assets/             // 静态资源
├── components/         // 公共组件
│   ├── ImageUploader.tsx  // 阶段一：上传组件
│   ├── ImageCropper.tsx   // 阶段二：裁剪编辑器
│   ├── ProcessingBar.tsx  // 阶段三：进度条
│   └── DxfViewer.tsx      // 阶段四：结果预览器
├── hooks/              // 自定义 Hooks
│   ├── usePolling.ts      // 封装轮询逻辑
│   └── useFileHandler.ts  // 处理文件校验和转换
├── pages/
│   └── Home/
│       ├── index.tsx      // 主入口，控制状态机流转
│       └── styles.less    // 样式
├── services/           // API 接口层
│   ├── api.ts             // Axios 实例
│   └── conversionApi.ts   // 具体的 convert, getStatus 接口
├── store/
│   └── appStore.ts        // 全局状态 (CurrentStep, FileData, ResultUrl)
├── utils/
│   └── canvasUtils.ts     // Canvas 辅助函数
└── App.tsx
```

## 2. 状态机设计 (核心逻辑)
整个页面就是一个状态机（State Machine）。我们在 `store/appStore.ts` 中定义唯一的数据源：

```typescript
type AppStep = 'UPLOAD' | 'EDIT' | 'PROCESSING' | 'RESULT';

interface AppState {
  step: AppStep;
  originalFile: File | null;      // 用户上传的原始文件
  croppedBlob: Blob | null;       // 裁剪后的图片数据
  taskId: string | null;          // 后端任务ID
  progress: number;               // 0-100
  dxfUrl: string | null;          // 最终结果地址

  // Actions
  setStep: (s: AppStep) => void;
  setFile: (f: File) => void;
  // … 其他setter
}
```

## 3. 后端接口协议约定 (API Contract)
为了实现进度条，我们不能使用单一的长连接请求，推荐使用 **“异步任务 + 轮询”** 模式：

1.  **提交任务** `POST /api/v1/image/convert`
    *   **Request:** `FormData` (包含名为 `file` 的图片文件)
    *   **Response:** `{ "code": 200, "data": { "taskId": "task_123456" } }`
2.  **查询进度** `GET /api/v1/image/status/{taskId}`
    *   前端每隔 1~2 秒调用一次。
    *   **Response (处理中):** `{ "status": "pending", "progress": 45, "msg": "正在边缘检测…" }`
    *   **Response (完成):** `{ "status": "completed", "progress": 100, "resultUrl": "http://…/result.dxf" }`
    *   **Response (失败):** `{ "status": "failed", "error": "图片过于模糊" }`

---

# 第四部分：实现方案

## 1. 最小可行性方案 (MVP)
**目标：** 因为 DXF 浏览器预览较难，MVP 阶段**暂不实现实时预览**，而是提供直接下载，或者由后端生成一张 SVG/PNG 作为预览图返回。

### 核心代码片段

#### A. 主控组件 (Home/index.tsx)
利用条件渲染控制界面流转。
```tsx
import { useAppStore } from '@/store/appStore';
import { ImageUploader } from '@/components/ImageUploader';
import { ImageCropper } from '@/components/ImageCropper';
import { ProcessingBar } from '@/components/ProcessingBar';
import { ResultView } from '@/components/ResultView';

const Home = () => {
  const step = useAppStore(s => s.step);

  return (
    <div className="main-container">
      {step === 'UPLOAD' && <ImageUploader />}
      {step === 'EDIT' && <ImageCropper />}
      {step === 'PROCESSING' && <ProcessingBar />}
      {step === 'RESULT' && <ResultView />}
    </div>
  );
};
```

#### B. 图片编辑器 (ImageCropper.tsx)
使用 `react-cropper` 获取裁剪后的 blob。
```tsx
import Cropper, { ReactCropperElement } from "react-cropper";
import "cropperjs/dist/cropper.css";
import { useRef } from "react";

export const ImageCropper = () => {
  const file = useAppStore(s => s.originalFile);
  const cropperRef = useRef<ReactCropperElement>(null);

  // 将 File 对象转为 URL 供 img 标签使用
  const imageUrl = useMemo(() => file ? URL.createObjectURL(file) : '', [file]);

  const handleConfirm = () => {
    const cropper = cropperRef.current?.cropper;
    if (cropper) {
      // 核心：获取裁切后的二进制数据
      cropper.getCroppedCanvas().toBlob((blob) => {
         // 调用上传逻辑
         uploadAndStartTask(blob);
      }, 'image/jpeg');
    }
  };

  return (
    <>
      <Cropper
        src={imageUrl}
        style={{ height: 500, width: "100%" }}
        ref={cropperRef}
        viewMode={1} 
        guides={true}
        rotatable={true}
      />
      <button onClick={() => cropperRef.current?.cropper.rotate(90)}>旋转</button>
      <button onClick={handleConfirm}>开始处理</button>
    </>
  );
}
```

#### C. 进度轮询逻辑
```ts
// 伪代码，放在 useEffect 或事件处理中
const startPolling = (taskId: string) => {
  const timer = setInterval(async () => {
    const res = await api.getStatus(taskId);
  
    // 更新进度条
    setStoreProgress(res.progress);

    if (res.status === 'completed') {
      clearInterval(timer);
      setStoreDxfUrl(res.resultUrl);
      setStoreStep('RESULT');
    } 
  
    if (res.status === 'failed') {
       clearInterval(timer);
       alert('失败: ' + res.error);
       setStoreStep('EDIT'); // 回退
    }
  }, 1000); // 1秒轮询一次
}
```

---

## 2. 后续扩展方案 (Phase 2)

**痛点解决：如何在 Web 端预览 DXF？**

浏览器无法直接渲染 `.dxf` 文件，必须进行解析。

### 方案 A：后端渲染 SVG (推荐，开发成本低)
*   **原理：** 后端在生成 DXF 的同时，顺便生成一个 SVG 文件（SVG 本质也是矢量，浏览器原生支持）。
*   **前端修改：** `ResultView` 组件中直接使用 `<img src={svgUrl} />` 展示。这能以最低成本实现“所见即所得”。

### 方案 B：前端 Three.js 渲染 (交互体验好，开发成本高)
*   **原理：** 前端下载 DXF 文本，在客户端解析并绘制到 Canvas 的 3D 场景中。
*   **技术栈：** `three` + `dxf-parser`。
*   **实现逻辑：**

```tsx
// DxfViewer.tsx 简单示意
import { DxfParser } from 'dxf-parser';
import * as THREE from 'three';

const DxfViewer = ({ dxfUrl }) => {
  useEffect(() => {
    // 1. 下载 DXF 文本
    fetch(dxfUrl).then(res => res.text()).then(text => {
        // 2. 解析
        const parser = new DxfParser();
        const dxfData = parser.parseSync(text);
      
        // 3. 将 dxfData 转换为 Three.js 的 LineSegments 几何体
        // 这部分代码比较复杂，通常需要遍历 dxfData.entities (LINE, LWPOLYLINE, ARC…)
        // 并为每个实体创建 THREE.BufferGeometry
      
        // 4. 初始化 Three Scene, Camera, Renderer 并挂载到 DOM
    });
  }, [dxfUrl]);

  return <div id="dxf-container" />;
}
```

## 总结建议

1.  **第一步**：使用 **React + Ant Design + react-cropper** 搭建基础界面。
2.  **第二步**：与后端联调，确保 `File -> Blob -> FormData -> Backend` 以及 `Polling` 机制通畅。
3.  **第三步**：先做 **“直接下载”** 功能，确保核心业务闭环。
4.  **第四步 (优化)**：请求后端同步返回一张 **SVG/PNG** 作为预览图，展示在下载按钮上方。这是性价比最高的“预览”方案。
