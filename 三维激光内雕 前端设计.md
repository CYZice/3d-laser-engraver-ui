# 三维激光内雕自助终端 (Kiosk) - 产品需求文档 (PRD) v2.0

> **文档状态**：Draft  
> **更新日期**：2026-03-07  
> **当前版本**：v2.0 (Kiosk Transformation)  
> **原版本**：v1.0 (Web Tool)

---

## 1. 背景与目标 (Context & Goals)

### 1.1 项目背景
原有的 Web 端图像处理工具已跑通基础逻辑。本项目旨在将其升级为**商业化自助终端（Kiosk）**，部署于商场、景区等高流量区域。
用户通过触摸屏自助完成“拍照 -> 编辑 -> 支付 -> 提交制作”的全流程。

### 1.2 核心目标
1.  **体验升级 (Experience)**：从“工具”转变为“沉浸式体验”。利用大屏可视化和实时交互吸引用户。
2.  **转化率 (Conversion)**：通过简化的流程和即时反馈（Live Preview），降低用户流失，促成冲动消费。
3.  **无人值守 (Self-service)**：通过清晰的引导和容错机制，最大程度减少人工干预。

### 1.3 关键约束 (Constraints)
*   **设备形态**：竖屏触摸一体机 (1080x1920 Portrait)。
*   **输入设备**：高清摄像头 (Webcam) + 触摸屏。
*   **支付环节**：当前阶段为 **Demo 演示模式**（模拟支付成功），暂不接入真实支付网关。
*   **输出方式**：提交至后端制作队列 (Engraving Queue)，不直接下载文件。

---

## 2. 用户故事 (User Stories)

| ID | 优先级 | 角色 | 场景/行为 (Given/When) | 期望结果 (Then) |
| :--- | :--- | :--- | :--- | :--- |
| **US-01** | **P0** | 路人 | 当我走过终端机时 | 屏幕应播放吸引人的动态视频（Attract Loop），明确告知“点击屏幕开始”。 |
| **US-02** | **P0** | 用户 | 当我点击开始后 | 系统应立即启动摄像头，显示我的实时影像，并引导我摆好姿势。 |
| **US-03** | **P0** | 用户 | 当我点击“拍照”按钮 | 系统应有 3-2-1 倒计时，然后定格画面供我确认。 |
| **US-04** | **P1** | 用户 | 如果我不满意刚才的照片 | 我可以一键“重拍”，无需重新加载页面。 |
| **US-05** | **P0** | 用户 | 当我确认照片后 | 我可以通过简单的触摸手势（拖拽、双指缩放）来调整照片的裁剪范围。 |
| **US-06** | **P0** | 用户 | 当我点击“确认制作” | 系统弹出支付二维码（Demo），模拟扫码后自动跳转至“制作中”状态。 |
| **US-07** | **P0** | 用户 | 当制作完成后 | 屏幕显示取货凭证号（Order ID），并提示预计等待时间，倒计时后自动返回首页。 |
| **US-08** | **P2** | 运维 | 当设备长时间无人操作 | 系统应自动重置到首页，清除上一位用户的隐私数据。 |

---

## 3. 详细功能需求 (Functional Requirements)

### 3.1 阶段一：吸引模式 (Attract Mode)
*   **UI/UX**：全屏播放 3D 激光内雕的精美展示视频或轮播图。
*   **交互**：屏幕任意位置点击，立即进入阶段二。
*   **技术点**：
    *   `<video>` 标签循环播放，静音或低音量。
    *   CSS 动画呼吸灯效果提示 "Touch to Start"。

### 3.2 阶段二：实时拍摄 (Live Capture)
*   **UI/UX**：
    *   大窗口显示摄像头预览流 (Mirror Mode 镜像)。
    *   辅助线（人脸轮廓虚线）引导用户对准位置。
    *   底部巨大的圆形“拍照”按钮。
*   **逻辑**：
    *   调用 `navigator.mediaDevices.getUserMedia`。
    *   **倒计时**：点击后显示 3s 倒计时动画，增加仪式感。
    *   **闪光灯模拟**：拍照瞬间屏幕瞬间白屏 (Flash Effect) 补光。
*   **异常处理**：若未检测到摄像头，提示“设备维护中”并允许回退到文件上传模式（备选）。

### 3.3 阶段三：编辑与预览 (Edit & Preview)
*   **UI/UX**：
    *   显示定格照片。
    *   手势操作：单指拖拽移动，双指缩放 (Pinch-to-zoom) 进行裁剪。
    *   按钮：“重拍” (左侧，红色/灰色)、“确认” (右侧，绿色/主色)。
*   **技术点**：
    *   使用 `react-cropper` 或原生 Canvas 实现触摸手势支持。
    *   **限制**：强制裁剪比例（如 1:1 或 3:4，根据水晶块形状锁定）。

### 3.4 阶段四：模拟支付 (Demo Payment)
*   **UI/UX**：
    *   展示订单预览图（处理后的效果示意图）。
    *   显示金额（如 ￥29.9）。
    *   展示一个静态二维码（WeChat/Alipay）。
*   **逻辑**：
    *   前端设置一个 `setTimeout` (3~5秒) 模拟“支付成功”回调。
    *   或者提供一个隐藏的“跳过支付”按钮供测试。
    *   支付成功后播放“支付成功”音效，跳转下一页。

### 3.5 阶段五：处理与提交 (Process & Queue)
*   **UI/UX**：
    *   高科技感的进度条/加载动画（粒子效果）。
    *   文案：“正在生成三维点云数据...”、“正在发送至激光雕刻机...”。
*   **逻辑**：
    *   上传裁剪后的图片至后端。
    *   轮询任务状态 (Polling)。
    *   接收后端返回的 `Order ID`。

### 3.6 阶段六：完成与重置 (Done & Reset)
*   **UI/UX**：
    *   显示大字号的 **取货码 (Order #888)**。
    *   提示：“请前往柜台领取您的水晶内雕”。
    *   倒计时 10秒 自动返回首页。
*   **数据清理**：
    *   **必须** 清除本地内存中的 Blob/File 对象。
    *   重置所有 State (Zustand store)。

---

## 4. 非功能需求 (Non-Functional)

### 4.1 性能与稳定性
*   **内存泄漏**：Kiosk 需 24/7 运行，必须确保摄像头流在使用后正确关闭 (`track.stop()`)，Canvas 及时释放。
*   **弱网处理**：商场网络可能不稳定，需具备断网重连机制，或在上传失败时提供友好的“重试”按钮。

### 4.2 交互体验 (Kiosk Specific)
*   **超时重置 (Idle Timeout)**：
    *   在非首页的任意界面，若用户无操作超过 60秒，弹出“您还在吗？”倒计时弹窗。
    *   倒计时结束（10秒）无响应，自动重置回首页（保护隐私）。
*   **触摸优化**：
    *   按钮最小尺寸：`80px * 80px`。
    *   禁用浏览器默认手势（长按菜单、双击缩放、右键菜单）。
    *   CSS: `user-select: none; touch-action: none;`

---

## 5. 验收标准 (Acceptance Criteria) - Gherkin

### Scenario: 用户完成一次正常的拍照下单流程
*   **Given** 终端处于“吸引模式”且摄像头连接正常。
*   **When** 我点击屏幕。
*   **Then** 我应该看到自己的实时影像。
*   **When** 我点击拍照并确认照片满意。
*   **And** 我在支付页面等待 5 秒。
*   **Then** 系统应提示“支付成功”并开始上传处理。
*   **When** 处理完成。
*   **Then** 我应看到一个取货码，且屏幕在 10 秒后自动变回“吸引模式”。

### Scenario: 用户中途离开
*   **Given** 我在“编辑页面”停留了 60 秒没有操作。
*   **When** 屏幕弹出超时提示。
*   **And** 我没有点击“继续”。
*   **Then** 系统应自动清空我的照片并返回首页。

---

## 6. 下一步开发计划 (Roadmap)

1.  **Phase 1 (Demo)**: 完成上述所有前端流程，支付使用 Mock，后端仅需接收文件并返回 Mock ID。
2.  **Phase 2 (Integration)**: 接入真实后端算法 API，展示真实的点云生成进度。
3.  **Phase 3 (Hardware)**: 接入真实支付 SDK，接入打印队列系统。
